---
plan: 01
title: "Project Structure & CSS Foundation"
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
autonomous: true

must_haves:
  truths:
    - "Project files exist in correct structure"
    - "CSS custom properties define brand colors"
    - "Google Fonts load Montserrat and Cormorant Garamond"
    - "Base styles apply dark background and white text"
  artifacts:
    - path: "index.html"
      provides: "HTML shell with font links and CSS/JS references"
      contains: "fonts.googleapis.com"
    - path: "css/styles.css"
      provides: "CSS foundation with reset, variables, base styles"
      contains: "--color-bg: #120E0B"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "link rel stylesheet"
      pattern: "link.*styles\\.css"
    - from: "index.html"
      to: "Google Fonts"
      via: "preconnect and font link"
      pattern: "fonts\\.googleapis\\.com"
---

<objective>
Create the project file structure and CSS foundation for the Antler Room website.

Purpose: Establish the base layer that all subsequent plans build upon - files, fonts, colors, and reset styles.
Output: Working index.html shell with CSS variables and base styles applied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-hero/01-RESEARCH.md

Key patterns from research:
- Google Fonts: Montserrat (400, 500, 600, 700) + Cormorant Garamond (400, italic)
- CSS custom properties under :root
- File structure: index.html, /css/styles.css, /js/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project file structure</name>
  <files>
    index.html
    css/styles.css
    js/main.js
  </files>
  <action>
Create the minimal file structure for the single-page site:

1. Create `css/` directory and empty `css/styles.css`
2. Create `js/` directory and empty `js/main.js`
3. Create `index.html` with:
   - DOCTYPE html, lang="en"
   - Meta charset UTF-8
   - Meta viewport for responsive
   - Title: "Antler Room | Independent Digital Record Label"
   - Google Fonts preconnect links (fonts.googleapis.com AND fonts.gstatic.com with crossorigin)
   - Google Fonts stylesheet link: `https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;1,400&family=Montserrat:wght@400;500;600;700&display=swap`
   - Link to css/styles.css
   - Empty body with comment placeholder
   - Script tag for js/main.js (before closing body, with defer)

Structure:
```
/Website AR
├── index.html
├── css/
│   └── styles.css
└── js/
    └── main.js
```
  </action>
  <verify>
    - `ls -la` shows index.html, css/, js/ directories
    - `cat index.html` shows Google Fonts links and CSS/JS references
  </verify>
  <done>File structure exists with index.html containing all required head elements</done>
</task>

<task type="auto">
  <name>Task 2: Implement CSS foundation</name>
  <files>css/styles.css</files>
  <action>
Write `css/styles.css` with these sections in order:

**1. CSS RESET (minimal)**
```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  scroll-behavior: smooth;
  scroll-padding-top: 80px;
}

body {
  min-height: 100vh;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

img, picture, video, canvas, svg {
  display: block;
  max-width: 100%;
}

input, button, textarea, select {
  font: inherit;
}

a {
  color: inherit;
  text-decoration: none;
}

ul, ol {
  list-style: none;
}
```

**2. CSS CUSTOM PROPERTIES**
```css
:root {
  /* Core Brand Colors */
  --color-bg: #120E0B;
  --color-white: #FFFFFF;
  --color-gold: #C5A059;

  /* Semantic Color Aliases */
  --color-text-primary: var(--color-white);
  --color-text-accent: var(--color-gold);
  --color-surface: var(--color-bg);
  --color-cta-bg: var(--color-gold);
  --color-cta-text: var(--color-bg);

  /* Typography */
  --font-sans: 'Montserrat', sans-serif;
  --font-serif: 'Cormorant Garamond', serif;

  /* Font Weights */
  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* Spacing Scale */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 1rem;
  --space-lg: 2rem;
  --space-xl: 4rem;
  --space-2xl: 8rem;

  /* Layout */
  --nav-height: 80px;
  --max-width: 1200px;
}
```

**3. BASE STYLES**
```css
body {
  background-color: var(--color-bg);
  color: var(--color-text-primary);
  font-family: var(--font-sans);
  font-weight: var(--font-weight-regular);
}
```

**4. TYPOGRAPHY**
```css
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  line-height: 1.2;
}
```

**5. UTILITY CLASSES** (for later use)
```css
.container {
  width: 100%;
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 0 var(--space-lg);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
```

Use clear section comment headers (e.g., `/* ================================= 1. CSS RESET ================================= */`)
  </action>
  <verify>
    - `cat css/styles.css` shows all sections
    - CSS contains `--color-bg: #120E0B` and `--color-gold: #C5A059`
    - CSS contains font-family definitions for Montserrat and Cormorant Garamond
  </verify>
  <done>CSS file contains reset, custom properties, base styles, typography, and utilities</done>
</task>

<task type="auto">
  <name>Task 3: Verify foundation renders</name>
  <files>index.html</files>
  <action>
Update index.html body to include a minimal test element that confirms styles are working:

```html
<body>
  <!-- Test element - remove after verification -->
  <div class="container" style="padding-top: 100px;">
    <h1>ANTLER Room</h1>
    <p>If you see this on a dark background with white text and the fonts look correct, CSS is working.</p>
  </div>

  <script src="js/main.js" defer></script>
</body>
```

This test element will be replaced in Plan 02 with actual navigation and hero content.
  </action>
  <verify>
    - Open index.html in browser
    - Background is dark charcoal (#120E0B)
    - Text is white
    - H1 shows in Montserrat Bold uppercase
    - Page loads without console errors
  </verify>
  <done>Foundation renders correctly - dark background, white text, Montserrat font visible</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. File structure exists: index.html, css/styles.css, js/main.js
2. Open index.html in browser - should show dark background (#120E0B)
3. Text should be white (#FFFFFF) in Montserrat font
4. No console errors
5. CSS custom properties defined and working
</verification>

<success_criteria>
- index.html exists with Google Fonts links and CSS/JS references
- css/styles.css contains CSS reset, custom properties (:root variables), base styles, typography rules
- Opening index.html shows dark charcoal background with white Montserrat text
- No 404 errors for CSS/JS/font files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hero/01-01-SUMMARY.md`
</output>
