---
plan: 02
title: "Navigation & Hero Section"
wave: 2
depends_on: ["01-01"]
files_modified:
  - index.html
  - css/styles.css
  - js/main.js
autonomous: true

must_haves:
  truths:
    - "User sees fixed navigation at top of page"
    - "Logo displays ANTLER (white) and Room (gold italic) on same baseline"
    - "Nav links for SUCCESS, STRATEGY, SERVICES, TEAM, GENRES, CONTACT are visible"
    - "SUBMIT DEMOS button is prominent in navigation"
    - "Clicking nav links scrolls smoothly to placeholder sections"
    - "Hero displays ANTLER ROOM header prominently"
    - "Hero shows subtitle and tagline text"
    - "Hero has primary CTA button"
  artifacts:
    - path: "index.html"
      provides: "Complete navigation and hero HTML structure"
      contains: "class=\"nav\""
    - path: "css/styles.css"
      provides: "Navigation and hero styling"
      contains: ".logo-antler"
    - path: "js/main.js"
      provides: "Smooth scroll functionality"
      contains: "scrollIntoView"
  key_links:
    - from: "nav links"
      to: "section IDs"
      via: "href anchors"
      pattern: "href=\"#(success|strategy|services|team|genres|contact)\""
    - from: "js/main.js"
      to: "anchor links"
      via: "querySelectorAll and click handler"
      pattern: "querySelectorAll.*href\\^=\"#\""
---

<objective>
Build the fixed navigation and hero section for the Antler Room website.

Purpose: Create the first-impression experience - users land on the page, see the brand identity, understand what Antler Room does, and can navigate to sections.
Output: Fully styled navigation with logo, links, CTA button, and hero section with header, taglines, and CTA.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-hero/01-RESEARCH.md
@.planning/phases/01-foundation-hero/01-01-SUMMARY.md (for CSS variables and structure)

Key patterns from research:
- Logo: inline-flex with align-items: baseline, gap: 0.15em
- Nav: position: fixed, z-index: 1000
- Smooth scroll: CSS scroll-behavior + JS scrollIntoView backup
- CTA: gold background, dark text, uppercase
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build navigation HTML and CSS</name>
  <files>
    index.html
    css/styles.css
  </files>
  <action>
**In index.html**, replace the test content with complete navigation structure:

```html
<body>
  <nav class="nav">
    <div class="nav-container">
      <a href="#" class="logo">
        <span class="logo-antler">ANTLER</span>
        <span class="logo-room">Room</span>
      </a>

      <ul class="nav-links">
        <li><a href="#success">SUCCESS</a></li>
        <li><a href="#strategy">STRATEGY</a></li>
        <li><a href="#services">SERVICES</a></li>
        <li><a href="#team">TEAM</a></li>
        <li><a href="#genres">GENRES</a></li>
        <li><a href="#contact">CONTACT</a></li>
      </ul>

      <a href="#submit-demos" class="nav-cta">SUBMIT DEMOS</a>
    </div>
  </nav>

  <!-- Hero and sections will follow -->

  <script src="js/main.js" defer></script>
</body>
```

**In css/styles.css**, add NAVIGATION section after existing styles:

```css
/* =================================
   6. NAVIGATION
   ================================= */

.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background-color: var(--color-bg);
  padding: var(--space-md) var(--space-lg);
}

.nav-container {
  max-width: var(--max-width);
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* Logo */
.logo {
  display: inline-flex;
  align-items: baseline;
  gap: 0.15em;
}

.logo-antler {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  font-size: 1.5rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-white);
}

.logo-room {
  font-family: var(--font-serif);
  font-style: italic;
  font-weight: var(--font-weight-regular);
  font-size: 1.5rem;
  color: var(--color-gold);
}

/* Nav Links */
.nav-links {
  display: flex;
  gap: var(--space-lg);
}

.nav-links a {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-medium);
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-white);
  transition: color 0.2s ease;
}

.nav-links a:hover {
  color: var(--color-gold);
}

.nav-links a:focus {
  outline: 2px solid var(--color-gold);
  outline-offset: 4px;
}

/* Nav CTA */
.nav-cta {
  display: inline-block;
  background-color: var(--color-cta-bg);
  color: var(--color-cta-text);
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 0.75rem 1.5rem;
  transition: opacity 0.2s ease;
}

.nav-cta:hover {
  opacity: 0.9;
}

.nav-cta:focus {
  outline: 2px solid var(--color-gold);
  outline-offset: 2px;
}
```
  </action>
  <verify>
    - Navigation is visible at top of page
    - Logo shows "ANTLER" in white Montserrat and "Room" in gold italic Cormorant Garamond
    - Six nav links are visible in uppercase
    - "SUBMIT DEMOS" button has gold background
    - Navigation stays fixed when scrolling (once content is added)
  </verify>
  <done>Navigation HTML complete with logo, 6 section links, and CTA button all styled</done>
</task>

<task type="auto">
  <name>Task 2: Build hero section HTML and CSS</name>
  <files>
    index.html
    css/styles.css
  </files>
  <action>
**In index.html**, add hero section after nav (inside body, before script):

```html
<header class="hero" id="hero">
  <div class="hero-content">
    <h1 class="hero-title">
      <span class="hero-title-antler">ANTLER</span>
      <span class="hero-title-room">Room</span>
    </h1>
    <p class="hero-subtitle">INDEPENDENT DIGITAL RECORD LABEL SHAPING GLOBAL CULTURE</p>
    <p class="hero-tagline">BUILT FOR DIGITALLY NATIVE AUDIENCES</p>
    <a href="#submit-demos" class="cta-button">SUBMIT YOUR DEMO</a>
  </div>
</header>

<main>
  <!-- Placeholder sections for smooth scroll targets -->
  <section id="success" class="section">
    <div class="container">
      <h2>SUCCESS</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="strategy" class="section">
    <div class="container">
      <h2>STRATEGY</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="services" class="section">
    <div class="container">
      <h2>SERVICES</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="team" class="section">
    <div class="container">
      <h2>TEAM</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="genres" class="section">
    <div class="container">
      <h2>GENRES</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="contact" class="section">
    <div class="container">
      <h2>CONTACT</h2>
      <p>Content coming in Phase 2</p>
    </div>
  </section>

  <section id="submit-demos" class="section">
    <div class="container">
      <h2>SUBMIT DEMOS</h2>
      <p>Demo submission form coming in Phase 2</p>
    </div>
  </section>
</main>
```

**In css/styles.css**, add HERO and SECTIONS after navigation:

```css
/* =================================
   7. HERO SECTION
   ================================= */

.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: var(--nav-height) var(--space-lg) var(--space-xl);
}

.hero-content {
  max-width: 800px;
}

.hero-title {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 0.2em;
  margin-bottom: var(--space-lg);
}

.hero-title-antler {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  font-size: clamp(3rem, 10vw, 6rem);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-white);
}

.hero-title-room {
  font-family: var(--font-serif);
  font-style: italic;
  font-weight: var(--font-weight-regular);
  font-size: clamp(3rem, 10vw, 6rem);
  color: var(--color-gold);
}

.hero-subtitle {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-semibold);
  font-size: clamp(0.875rem, 2vw, 1.125rem);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--color-white);
  margin-bottom: var(--space-md);
}

.hero-tagline {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-regular);
  font-size: clamp(0.75rem, 1.5vw, 0.875rem);
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: var(--color-gold);
  margin-bottom: var(--space-xl);
}

/* =================================
   8. CTA BUTTON
   ================================= */

.cta-button {
  display: inline-block;
  background-color: var(--color-cta-bg);
  color: var(--color-cta-text);
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 1rem 2rem;
  transition: opacity 0.2s ease;
}

.cta-button:hover {
  opacity: 0.9;
}

.cta-button:focus {
  outline: 2px solid var(--color-gold);
  outline-offset: 2px;
}

/* =================================
   9. SECTIONS (Placeholder styles)
   ================================= */

.section {
  min-height: 50vh;
  padding: var(--space-2xl) 0;
  scroll-margin-top: var(--nav-height);
}

.section h2 {
  margin-bottom: var(--space-lg);
}
```
  </action>
  <verify>
    - Hero section fills viewport height
    - "ANTLER Room" title is large and centered (ANTLER white, Room gold italic)
    - Subtitle "INDEPENDENT DIGITAL..." is visible below title
    - Tagline "BUILT FOR DIGITALLY..." is visible in gold
    - CTA button "SUBMIT YOUR DEMO" has gold background
    - Placeholder sections exist with IDs matching nav links
  </verify>
  <done>Hero section displays title, subtitle, tagline, and CTA. Placeholder sections exist for all nav targets.</done>
</task>

<task type="auto">
  <name>Task 3: Implement smooth scroll JavaScript</name>
  <files>js/main.js</files>
  <action>
**In js/main.js**, add smooth scroll handler:

```javascript
/**
 * Antler Room - Main JavaScript
 * Handles smooth scrolling navigation
 */

document.addEventListener('DOMContentLoaded', () => {
  // Smooth scroll for anchor links
  // CSS scroll-behavior: smooth handles most cases,
  // but this provides fallback and better control

  const anchorLinks = document.querySelectorAll('a[href^="#"]');

  anchorLinks.forEach(link => {
    link.addEventListener('click', function(e) {
      const href = this.getAttribute('href');

      // Skip if just "#" (logo link to top)
      if (href === '#') {
        e.preventDefault();
        window.scrollTo({
          top: 0,
          behavior: 'smooth'
        });
        return;
      }

      const targetElement = document.querySelector(href);

      if (targetElement) {
        e.preventDefault();
        targetElement.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
});
```

This JavaScript:
1. Waits for DOM to load
2. Finds all anchor links (href starts with #)
3. Adds click handlers that prevent default and use scrollIntoView
4. Handles logo click (href="#") to scroll to top
5. Works with CSS scroll-padding-top already set in styles.css
  </action>
  <verify>
    - Click any nav link (e.g., SUCCESS) - page smoothly scrolls to that section
    - Section header is visible (not hidden under fixed nav)
    - Click logo - page scrolls to top
    - Click hero CTA - scrolls to submit-demos section
    - No console errors
  </verify>
  <done>Smooth scroll works for all navigation links, logo, and CTA buttons</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Navigation is fixed at top with logo (ANTLER white, Room gold italic)
2. Six nav links visible: SUCCESS, STRATEGY, SERVICES, TEAM, GENRES, CONTACT
3. "SUBMIT DEMOS" CTA button prominent in nav with gold background
4. Hero displays large "ANTLER Room" title centered
5. Subtitle and tagline visible below title
6. Primary CTA button in hero
7. Clicking any nav link smoothly scrolls to placeholder section
8. Section content visible (not hidden under nav) after scroll
9. No console errors
</verification>

<success_criteria>
**Requirements satisfied:**
- NAV-01: Fixed navigation menu with section links - DONE
- NAV-02: Prominent CTA button "SUBMIT DEMOS" in navigation - DONE
- NAV-03: Smooth scroll navigation to sections - DONE
- HERO-01: Display "ANTLER ROOM" header prominently - DONE
- HERO-02: Display sub-header about independent digital record label - DONE
- HERO-03: Display tagline about digitally native audiences - DONE
- HERO-04: Primary CTA button linking to demo submission - DONE
- VIS-01: Dark charcoal/black background throughout - DONE (from Plan 01)
- VIS-02: Gold/beige accent colors for highlights, buttons, links - DONE
- VIS-03: All-caps sans-serif typography for headers - DONE
- VIS-04: Clean sans-serif body typography - DONE

**Phase 1 Success Criteria:**
1. User lands on page and sees ANTLER ROOM hero with dark background and gold accents - VERIFIED
2. Navigation is visible and clicking links scrolls smoothly to sections - VERIFIED
3. "SUBMIT DEMOS" CTA is prominent in nav - VERIFIED
4. Typography follows brand guidelines - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-hero/01-02-SUMMARY.md`
</output>
