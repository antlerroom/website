---
plan: 02-03
title: "Genres & Partners Sections"
wave: 2
depends_on: ["02-01"]
files_modified:
  - index.html
  - css/styles.css
autonomous: true

must_haves:
  truths:
    - "User sees 'OUR GENRES' header with multigenre copy"
    - "User sees 6 genre tags displayed as pills"
    - "Genre tags are: UK RAP, ALTERNATIVE, ELECTRONIC, AMBIENT, NEOCLASSICAL, POP"
    - "User sees 3 partners with descriptions"
    - "Partners are: SoundOn (Distribution), Downtown (Sync), Well Overdue (Publishing)"
  artifacts:
    - path: "index.html"
      provides: "Genres and Partners section HTML"
      contains: "genre-tags"
    - path: "css/styles.css"
      provides: "Genre tag and partner card styles"
      contains: ".genre-tag"
  key_links:
    - from: "index.html#genres"
      to: "css/styles.css"
      via: ".genre-tags, .genre-tag classes"
      pattern: "genre-tag"
---

<objective>
Build the Genres and Partners sections with pill-style genre tags and partner display cards.

Purpose: Genres show what music Antler Room works with; Partners establish credibility through industry relationships.
Output: Two complete content sections with tag and card layouts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections-form/02-RESEARCH.md

Dependencies:
- 02-01 creates section-header-group and section-header CSS classes
- If 02-01 not run, include those styles

Key patterns established:
- CSS custom properties in use
- Section pattern with scroll-margin-top
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Genres Section HTML and CSS</name>
  <files>index.html, css/styles.css</files>
  <action>
Replace the #genres section placeholder with complete Genres section.

HTML structure for #genres section:
```html
<section id="genres" class="section">
  <div class="container">
    <header class="section-header-group">
      <h2 class="section-header">OUR GENRES</h2>
      <p class="section-subheader">We're multigenre specialists. From the raw energy of UK rap to the atmospheric depths of ambient and neoclassical, we understand the nuances that make each genre tick. Our approach is genre-agnostic but culturally informed.</p>
    </header>

    <ul class="genre-tags">
      <li class="genre-tag">UK RAP</li>
      <li class="genre-tag">ALTERNATIVE</li>
      <li class="genre-tag">ELECTRONIC</li>
      <li class="genre-tag">AMBIENT</li>
      <li class="genre-tag">NEOCLASSICAL</li>
      <li class="genre-tag">POP</li>
    </ul>
  </div>
</section>
```

Add CSS for genre tags (add to "10. CONTENT SECTIONS" block):

```css
/* Genre Tags */
.genre-tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
  justify-content: center;
}

.genre-tag {
  display: inline-block;
  padding: 0.5rem 1.25rem;
  border: 1px solid var(--color-gold);
  border-radius: 100px;
  font-family: var(--font-sans);
  font-weight: var(--font-weight-semibold);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-gold);
  transition: all 0.2s ease;
}

.genre-tag:hover {
  background-color: var(--color-gold);
  color: var(--color-bg);
}
```
  </action>
  <verify>
Open index.html in browser. The #genres section shows:
- "OUR GENRES" header centered with subheader
- 6 genre tags displayed as pills in a centered flex layout
- Tags have gold border, hover fills with gold
- Tags wrap naturally on smaller screens
  </verify>
  <done>
Genres section displays 6 genre tags as pill-shaped badges. Tags use gold borders with hover fill effect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Partners Section HTML and CSS</name>
  <files>index.html, css/styles.css</files>
  <action>
The #genres section currently exists but we need a Partners section. Add a new section AFTER the genres section but BEFORE the contact section.

First, locate where #genres section ends and add new #partners section before #contact.

HTML structure for new #partners section (add after #genres, before #contact):
```html
<section id="partners" class="section">
  <div class="container">
    <header class="section-header-group">
      <h2 class="section-header">OUR PARTNERS</h2>
      <p class="section-subheader">We've built strategic relationships with industry leaders to ensure our artists have access to the best distribution, sync, and publishing opportunities available.</p>
    </header>

    <div class="partners-grid">
      <article class="partner-card">
        <h3 class="partner-name">SOUNDON</h3>
        <p class="partner-description">Distribution</p>
        <p class="partner-detail">Global distribution platform with priority access to playlist placements and promotional opportunities across all major streaming services.</p>
      </article>
      <article class="partner-card">
        <h3 class="partner-name">DOWNTOWN</h3>
        <p class="partner-description">Sync</p>
        <p class="partner-detail">Industry-leading sync licensing connecting our catalog to film, television, advertising, and gaming opportunities worldwide.</p>
      </article>
      <article class="partner-card">
        <h3 class="partner-name">WELL OVERDUE</h3>
        <p class="partner-description">Publishing</p>
        <p class="partner-detail">Publishing administration and creative development ensuring our artists maximize their songwriting revenue and opportunities.</p>
      </article>
    </div>
  </div>
</section>
```

Add CSS for partners grid (add to "10. CONTENT SECTIONS" block):

```css
/* Partners Grid */
.partners-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: var(--space-lg);
  text-align: center;
}

.partner-card {
  padding: var(--space-lg);
}

.partner-name {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-bold);
  font-size: 1.25rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-white);
  margin-bottom: var(--space-xs);
}

.partner-description {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-semibold);
  font-size: 0.75rem;
  color: var(--color-gold);
  text-transform: uppercase;
  letter-spacing: 0.1em;
  margin-bottom: var(--space-md);
}

.partner-detail {
  font-family: var(--font-sans);
  font-weight: var(--font-weight-regular);
  font-size: 0.875rem;
  line-height: 1.7;
  color: var(--color-text-primary);
}
```
  </action>
  <verify>
Open index.html in browser. Scroll past genres to find new partners section:
- "OUR PARTNERS" header with subheader
- 3 partner cards (SoundOn, Downtown, Well Overdue)
- Each card shows name, role (Distribution/Sync/Publishing), and description
- Partners grid responds to screen size
  </verify>
  <done>
Partners section added with 3 partner cards. Each partner has name, type, and description displayed in a responsive grid.
  </done>
</task>

</tasks>

<verification>
1. Open index.html in browser
2. Navigate to #genres section - verify 6 genre tags display as pills
3. Hover over genre tags - verify gold fill effect
4. Scroll to #partners section - verify 3 partner cards display
5. Verify partner types: Distribution, Sync, Publishing
6. Resize browser window - verify both layouts respond
7. Run: grep -n "genre-tag\|partner-card" css/styles.css (should find both)
</verification>

<success_criteria>
- GENRE-01: "OUR GENRES" section header visible
- GENRE-02: Body copy about multigenre specialists present
- GENRE-03: All 6 genre tags displayed (UK RAP, ALTERNATIVE, ELECTRONIC, AMBIENT, NEOCLASSICAL, POP)
- PART-01: Partner names visible (SoundOn, Downtown, Well Overdue)
- PART-02: Partner descriptions present (Distribution, Sync, Publishing roles)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections-form/02-03-SUMMARY.md`
</output>
